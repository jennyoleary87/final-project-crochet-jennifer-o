export default class ProjectDetails {
    constructor(id, description, timeSpent, yarn, hook) {
        this.id = id;
        this.description = description;
        this.timeSpent = timeSpent;
        this.yarn = yarn;
        this.hook = hook;
    }
}